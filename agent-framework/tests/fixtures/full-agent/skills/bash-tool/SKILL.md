---
name: "bash-tool"
description: "Execute bash commands and return results - used for E2E testing"
license: "MIT"
metadata:
  author: "test"
  version: "1.0.0"
allowed-tools: ["bash"]
---

# Bash Tool Skill

This skill allows the agent to execute bash commands.

## When to Use

When the user asks you to run a bash command.

## Usage

Use this skill to run shell commands like `ls`, `echo`, `pwd`, `cat`, etc.
