server: bench
vendor: bench
version: "1.0.0"

connection:
  type: streamableHttp
  url: http://172.17.0.1:18082/mcp
  timeout: 60

# 压测 MCP 为只读语义，避免 HITL 拦截把请求打成 permission_ask 暂停
permissions:
  read_only: true
  tools:
    bench_echo: allow
