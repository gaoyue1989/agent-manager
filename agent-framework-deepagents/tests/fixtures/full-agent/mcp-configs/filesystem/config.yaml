vendor: "block"
server: "filesystem"
version: "1.0.0"

connection:
  type: "sse"
  url: "http://localhost:8811/sse"
  timeout: 60

permissions:
  allow_paths: ["/workspace", "/tmp", "/config"]
  deny_paths: ["/system", "/etc", "/root"]
  read_only: false
