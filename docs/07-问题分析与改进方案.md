# 实测问题分析与改进方案（基于 agent-framework 源码）

版本: v1.1
日期: 2026-08-07
关联: 07-实测问题清单.md

---

## 1. 问题全景分析

### 1.1 问题分布

| 优先级 | 数量 | 占比 | 核心影响 |
|--------|------|------|----------|
| **P1** | 5 | 38% | 协议合规、功能正确性 |
| **P2** | 5 | 38% | 信息失真、一致性 |
| **P3** | 3 | 23% | 设计僵硬、扩展性 |

### 1.2 根因聚类

```
┌─────────────────────────────────────────────────────────────────┐
│                     问题根因聚类分析                              │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │  A2A协议    │  │  会话管理   │  │  工具系统   │              │
│  │  实现偏差   │  │  设计缺陷   │  │  混乱       │              │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘              │
│         │               │               │                       │
│         ▼               ▼               ▼                       │
│  ┌─────────────────────────────────────────────────┐            │
│  │         根本原因：框架层缺乏标准化设计             │            │
│  │  • 未严格遵循 A2A 1.0 规范                       │            │
│  │  • 自定义会话模型与标准不兼容                     │            │
│  │  • 工具分类与暴露机制缺失                         │            │
│  └─────────────────────────────────────────────────┘            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. P1 级缺陷修复方案

### 2.1 A2A 协议参数解析不兼容 (缺陷 1.1)

**问题代码：** `A2AController.java:171-203`

```java
// 当前实现 - 只读 metadata
private String resolveUserId(Map<String, Object> params) {
    var metadata = (Map<String, Object>) params.get("metadata");  // 行 172
    if (metadata != null) {
        var uid = metadata.get("userId");  // 只看 metadata.userId
        if (uid != null && !uid.toString().isBlank()) {
            return uid.toString();
        }
    }
    return null;  // 忽略顶层 userId
}
```

**修复方案：** `A2AController.java`

```java
@SuppressWarnings("unchecked")
private String resolveUserId(Map<String, Object> params) {
    // 1. 优先读取标准 A2A 顶层参数
    Object topLevelUserId = params.get("userId");
    if (topLevelUserId != null && !topLevelUserId.toString().isBlank()) {
        return topLevelUserId.toString();
    }
    
    // 2. 兼容旧格式 metadata.userId
    var metadata = (Map<String, Object>) params.get("metadata");
    if (metadata != null) {
        var uid = metadata.get("userId");
        if (uid != null && !uid.toString().isBlank()) {
            log.warn("Deprecated: using metadata.userId, migrate to top-level userId");
            return uid.toString();
        }
    }
    
    return null; // 回退到默认 vendorKey
}

@SuppressWarnings("unchecked")
private String resolveThreadId(Map<String, Object> params, Map<String, Object> message) {
    // 1. 优先读取标准 A2A 顶层参数
    Object sessionId = params.get("sessionId");
    if (sessionId != null && !sessionId.toString().isBlank()) {
        return sessionId.toString();
    }
    
    Object conversationId = params.get("conversationId");
    if (conversationId != null && !conversationId.toString().isBlank()) {
        return conversationId.toString();
    }
    
    // 2. 兼容旧格式 metadata
    var metadata = (Map<String, Object>) params.get("metadata");
    if (metadata != null) {
        var tid = metadata.get("thread_id");
        if (tid != null && !tid.toString().isBlank()) {
            log.warn("Deprecated: using metadata.thread_id, migrate to top-level sessionId");
            return tid.toString();
        }
        var ctxId = metadata.get("contextId");
        if (ctxId != null && !ctxId.toString().isBlank()) {
            return ctxId.toString();
        }
    }
    
    // 3. message.taskId (兼容)
    var taskId = message.get("taskId");
    if (taskId != null && !taskId.toString().isBlank()) {
        return taskId.toString();
    }
    
    // 4. 自动生成
    return UUID.randomUUID().toString();
}
```

**验收标准：**
- [ ] 标准 A2A 顶层参数 (userId/sessionId/conversationId) 优先读取
- [ ] 旧格式 metadata 参数向后兼容，输出 deprecation 警告
- [ ] 缺失参数时自动生成 UUID 并记录日志

---

### 2.2 会话续接缺陷 (缺陷 1.2)

**问题代码：** `AgentRuntimeService.java:90-92`

```java
if (threadId == null || threadId.isEmpty()) {
    threadId = UUID.randomUUID().toString();  // 每次生成新 UUID
}
```

**修复方案：** 新增 `SessionManager.java`

```java
package io.agentmanager.framework.service;

import java.time.Duration;
import java.time.Instant;
import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

@Service
public class SessionManager {
    private static final Logger log = LoggerFactory.getLogger(SessionManager.class);
    private static final Duration DEFAULT_TTL = Duration.ofDays(30);
    
    // sessionKey -> SessionState
    private final Map<String, SessionState> sessions = new ConcurrentHashMap<>();
    
    public record SessionState(
        String sessionKey,
        String userId,
        String threadId,
        Instant createdAt,
        Instant expiresAt
    ) {
        public boolean isExpired() {
            return Instant.now().isAfter(expiresAt);
        }
    }
    
    /**
     * 获取或创建会话
     */
    public SessionState getOrCreateSession(String userId, String sessionId) {
        if (sessionId == null || sessionId.isBlank()) {
            // 无 sessionId 时创建新会话
            String newThreadId = UUID.randomUUID().toString();
            return createSession(userId, newThreadId);
        }
        
        String sessionKey = buildSessionKey(userId, sessionId);
        SessionState existing = sessions.get(sessionKey);
        
        if (existing != null && !existing.isExpired()) {
            log.debug("Resuming session: {}", sessionKey);
            return existing;
        }
        
        return createSession(userId, sessionId);
    }
    
    private SessionState createSession(String userId, String threadId) {
        String uid = userId != null ? userId : "anonymous";
        String sessionKey = uid + ":" + threadId;
        
        SessionState newSession = new SessionState(
            sessionKey, userId, threadId,
            Instant.now(), Instant.now().plus(DEFAULT_TTL)
        );
        
        sessions.put(sessionKey, newSession);
        log.info("Created session: {}", sessionKey);
        return newSession;
    }
    
    private String buildSessionKey(String userId, String sessionId) {
        String uid = userId != null ? userId : "anonymous";
        return uid + ":" + sessionId;
    }
    
    /**
     * 清理过期会话
     */
    public int cleanupExpired() {
        Instant now = Instant.now();
        int count = 0;
        
        var it = sessions.entrySet().iterator();
        while (it.hasNext()) {
            if (it.next().getValue().isExpired()) {
                it.remove();
                count++;
            }
        }
        
        if (count > 0) log.info("Cleaned {} expired sessions", count);
        return count;
    }
    
    public int getActiveCount() {
        return (int) sessions.values().stream()
            .filter(s -> !s.isExpired())
            .count();
    }
}
```

**修改 `AgentRuntimeService.java` 集成 SessionManager：**

```java
public class AgentRuntimeService {
    private final SessionManager sessionManager;
    
    public AgentRuntimeService(
        OafConfig oafConfig,
        HarnessAgent agent,
        List<Map<String, Object>> mcpConfigs,
        LLMLogger llmLogger,
        SessionManager sessionManager
    ) {
        this.oafConfig = oafConfig;
        this.tenantPrefix = oafConfig.slug();
        this.agent = agent;
        this.mcpConfigs = mcpConfigs;
        this.llmLogger = llmLogger;
        this.sessionManager = sessionManager;
    }
    
    public Map<String, Object> invoke(String message, String threadId, String userId) {
        var resolvedUserId = resolveUserId(userId);
        
        // 通过 SessionManager 管理会话
        var session = sessionManager.getOrCreateSession(resolvedUserId, threadId);
        var fullThreadId = makeThreadId(session.threadId());
        
        try {
            var ctx = RuntimeContext.builder()
                .sessionId(fullThreadId)
                .userId(resolvedUserId)
                .build();
            
            var userMsg = new UserMessage("user", message);
            var result = agent.call(List.of(userMsg), ctx).block();
            
            var responseText = result != null ? result.getTextContent() : "";
            return Map.of("response", responseText, "thread_id", session.threadId());
        } catch (Exception e) {
            log.error("invoke failed: {}", e.getMessage(), e);
            return Map.of("response", "[Agent:" + name() + "] Error: " + e.getMessage(),
                         "thread_id", session.threadId());
        }
    }
}
```

---

### 2.3 会话无回收机制 (缺陷 1.3)

**修复方案：** 新增 `SessionCleanupService.java`

```java
package io.agentmanager.framework.service;

import java.time.Instant;
import java.time.temporal.ChronoUnit;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Service;

@Service
public class SessionCleanupService {
    private static final Logger log = LoggerFactory.getLogger(SessionCleanupService.class);
    private static final int RETENTION_DAYS = 7;
    
    private final JdbcTemplate jdbcTemplate;
    private final SessionManager sessionManager;
    
    public SessionCleanupService(JdbcTemplate jdbcTemplate, SessionManager sessionManager) {
        this.jdbcTemplate = jdbcTemplate;
        this.sessionManager = sessionManager;
    }
    
    @Scheduled(cron = "0 0 3 * * ?") // 每天凌晨3点执行
    public void cleanup() {
        log.info("Starting session cleanup...");
        
        // 1. 内存中的过期会话
        int memCleaned = sessionManager.cleanupExpired();
        
        // 2. 数据库中的过期记录
        Instant cutoff = Instant.now().minus(RETENTION_DAYS, ChronoUnit.DAYS);
        
        int stateCleaned = 0;
        int fsCleaned = 0;
        try {
            stateCleaned = jdbcTemplate.update(
                "DELETE FROM agent_state WHERE updated_at < ?", cutoff);
            fsCleaned = jdbcTemplate.update(
                "DELETE FROM agent_fs WHERE updated_at < ?", cutoff);
        } catch (Exception e) {
            log.warn("DB cleanup failed: {}", e.getMessage());
        }
        
        log.info("Cleanup done: memory={}, agent_state={}, agent_fs={}",
            memCleaned, stateCleaned, fsCleaned);
    }
}
```

**启用定时任务：** `AgentFrameworkApplication.java`

```java
@SpringBootApplication
@EnableScheduling  // 新增
public class AgentFrameworkApplication {
    // ...
}
```

---

### 2.4 `/tools` 端点硬编码返回空 (缺陷 1.4)

**问题代码：** `ToolController.java:50-57`

```java
@GetMapping("/tools")
public Map<String, Object> listTools() {
    return Map.of(
        "builtin", oafConfig.tools(),
        "custom", List.of(),
        "mcp", List.of()  // 硬编码空列表
    );
}
```

**修复方案：** `ToolController.java`

```java
@RestController
public class ToolController {
    private final OafConfig oafConfig;
    private final AgentRuntimeService agentRuntime;
    private final List<Map<String, Object>> mcpConfigs;
    private final McpManager mcpManager;
    private final McpToolRegistrar mcpToolRegistrar;  // 新增依赖

    public ToolController(
        OafConfig oafConfig,
        AgentRuntimeService agentRuntime,
        List<Map<String, Object>> mcpConfigs,
        McpManager mcpManager,
        McpToolRegistrar mcpToolRegistrar
    ) {
        this.oafConfig = oafConfig;
        this.agentRuntime = agentRuntime;
        this.mcpConfigs = mcpConfigs;
        this.mcpManager = mcpManager;
        this.mcpToolRegistrar = mcpToolRegistrar;
    }

    @GetMapping("/tools")
    public Map<String, Object> listTools(
        @RequestParam(defaultValue = "false") boolean includeInternal
    ) {
        List<Map<String, Object>> tools = new ArrayList<>();
        
        // MCP 工具
        List<Map<String, Object>> mcpTools = getMcpTools();
        tools.addAll(mcpTools);
        
        // 内置工具（可选）
        if (includeInternal) {
            oafConfig.tools().stream()
                .map(name -> Map.<String, Object>of("name", name, "category", "internal"))
                .forEach(tools::add);
        }
        
        return Map.of("tools", tools, "totalCount", tools.size(), "mcpCount", mcpTools.size());
    }
    
    private List<Map<String, Object>> getMcpTools() {
        List<Map<String, Object>> result = new ArrayList<>();
        for (var config : mcpConfigs) {
            String serverName = (String) config.getOrDefault("server", "unknown");
            mcpToolRegistrar.getToolsByServer(serverName).stream()
                .map(tool -> Map.<String, Object>of(
                    "name", tool.getName(),
                    "server", serverName,
                    "category", "mcp",
                    "description", tool.getDescription() != null ? tool.getDescription() : ""
                ))
                .forEach(result::add);
        }
        return result;
    }
    
    @GetMapping("/mcp")
    public List<Map<String, Object>> listMcp() {
        return mcpManager.getMcpSummaries(mcpConfigs);
    }
    
    @GetMapping("/skills")
    public List<Map<String, Object>> listSkills() {
        return oafConfig.skills().stream()
            .map(s -> Map.<String, Object>of(
                "name", s.name(),
                "description", s.description() != null ? s.description() : "",
                "version", s.version(),
                "source", s.source()
            ))
            .toList();
    }
}
```

**McpToolRegistrar 新增方法：**

```java
// McpToolRegistrar.java
public List<ToolInfo> getToolsByServer(String serverName) {
    return registeredTools.entrySet().stream()
        .filter(e -> e.getKey().startsWith(serverName + ":"))
        .map(Map.Entry::getValue)
        .collect(Collectors.toList());
}

public record ToolInfo(String name, String description, String serverName) {}
```

---

### 2.5 ActiveMCP.json 导致 `/mcp` 500 (缺陷 1.5)

**问题代码：** `McpManager.java:51-59, 77-93`

```java
// 行 55: readTree() 返回 JsonNode
config.put("tools", mapper.readTree(activeMcp.toFile()));

// 行 83: 强转 Map 失败
var tools = (Map<String, Object>) mc.getOrDefault("tools", Map.of());  // ClassCastException!
```

**修复方案：** `McpManager.java`

```java
@Service
public class McpManager {
    private static final Logger log = LoggerFactory.getLogger(McpManager.class);
    private final Path mcpConfigsDir;
    private final com.fasterxml.jackson.databind.ObjectMapper mapper;

    public McpManager(java.nio.file.Path configDir) {
        this.mcpConfigsDir = configDir;
        this.mapper = new com.fasterxml.jackson.databind.ObjectMapper();
    }

    @SuppressWarnings("unchecked")
    private Map<String, Object> loadSingleConfig(McpServerConfig ms) {
        var mcpDir = mcpConfigsDir.resolve(ms.configDir().isEmpty() ? ms.server() : ms.configDir());
        if (!mcpDir.toFile().exists()) {
            mcpDir = mcpConfigsDir.resolve(ms.server());
        }
        if (!mcpDir.toFile().exists()) {
            return null;
        }

        var config = new java.util.LinkedHashMap<String, Object>();
        config.put("vendor", ms.vendor());
        config.put("server", ms.server());
        config.put("version", ms.version());
        config.put("required", ms.required());

        // 修复: ActiveMCP.json 安全加载
        var activeMcp = mcpDir.resolve("ActiveMCP.json");
        if (activeMcp.toFile().exists()) {
            try {
                var jsonNode = mapper.readTree(activeMcp.toFile());
                config.put("tools", safeJsonNodeToMap(jsonNode));
            } catch (Exception e) {
                log.warn("Failed to load ActiveMCP.json for {}: {}", ms.server(), e.getMessage());
                config.put("tools", Map.of());
            }
        }

        var configYaml = mcpDir.resolve("config.yaml");
        if (configYaml.toFile().exists()) {
            try {
                var yaml = new org.yaml.snakeyaml.Yaml();
                var yamlData = (Map<String, Object>) yaml.load(
                    configYaml.toFile().toURI().toURL().openStream());
                if (yamlData != null && yamlData.containsKey("connection")) {
                    config.put("connection", yamlData.get("connection"));
                }
            } catch (Exception e) {
                log.warn("Failed to load config.yaml for {}: {}", ms.server(), e.getMessage());
            }
        }

        return config;
    }
    
    private Map<String, Object> safeJsonNodeToMap(com.fasterxml.jackson.databind.JsonNode node) {
        if (node == null || node.isNull()) return Map.of();
        if (!node.isObject()) return Map.of("value", safeJsonNodeToValue(node));
        
        var result = new java.util.LinkedHashMap<String, Object>();
        node.fields().forEachRemaining(e -> result.put(e.getKey(), safeJsonNodeToValue(e.getValue())));
        return result;
    }
    
    private Object safeJsonNodeToValue(com.fasterxml.jackson.databind.JsonNode node) {
        if (node == null || node.isNull()) return null;
        if (node.isTextual()) return node.asText();
        if (node.isBoolean()) return node.asBoolean();
        if (node.isNumber()) return node.isLong() ? node.asLong() : node.asDouble();
        if (node.isArray()) {
            var list = new ArrayList<>();
            for (var item : node) list.add(safeJsonNodeToValue(item));
            return list;
        }
        if (node.isObject()) return safeJsonNodeToMap(node);
        return node.toString();
    }

    public List<Map<String, Object>> getMcpSummaries(List<Map<String, Object>> mcpConfigs) {
        var summaries = new ArrayList<Map<String, Object>>();
        for (var mc : mcpConfigs) {
            try {
                var conn = (Map<String, Object>) mc.getOrDefault("connection", Map.of());
                
                // 修复: 安全获取 tool_count
                int toolCount = 0;
                var toolsObj = mc.get("tools");
                if (toolsObj instanceof Map<?, ?> toolsMap) {
                    var selected = toolsMap.get("selectedTools");
                    if (selected instanceof List<?> list) toolCount = list.size();
                } else if (toolsObj instanceof com.fasterxml.jackson.databind.JsonNode node) {
                    if (node.has("selectedTools")) toolCount = node.get("selectedTools").size();
                }
                
                summaries.add(Map.of(
                    "server", mc.getOrDefault("server", "unknown"),
                    "vendor", mc.getOrDefault("vendor", ""),
                    "connection_type", conn.getOrDefault("type", "N/A"),
                    "url", conn.getOrDefault("url", "N/A"),
                    "tool_count", toolCount
                ));
            } catch (Exception e) {
                log.warn("Summary failed for {}: {}", mc.get("server"), e.getMessage());
                summaries.add(Map.of(
                    "server", mc.getOrDefault("server", "unknown"),
                    "error", e.getMessage()
                ));
            }
        }
        return summaries;
    }
}
```

---

## 3. P2 级缺陷修复方案

### 3.1 `/mcp` 的 `tool_count: 0` 失真 (缺陷 2.1)

已在 2.5 中修复，通过安全的类型检查获取 `selectedTools` 大小。

---

### 3.2 内置工具与业务工具无区分 (缺陷 2.2)

**修复方案：** `AgentRuntimeService.java` 添加工具过滤

```java
// 内置工具黑名单
private static final Set<String> INTERNAL_TOOLS = Set.of(
    "agent_send", "agent_spawn", "agent_list",
    "plan_enter", "plan_write", "plan_exit",
    "skill_manage", "load_skill_through_path",
    "task_list", "task_output", "task_cancel", "wait_async_results"
);

public List<String> getToolsForLLM() {
    return oafConfig.tools().stream()
        .filter(name -> !INTERNAL_TOOLS.contains(name))
        .collect(Collectors.toList());
}
```

---

### 3.3 会话键三端点三种格式 (缺陷 2.3)

**修复方案：** 统一会话键格式

```java
// AgentRuntimeService.java

/**
 * 统一外部会话键: {userId}:{sessionId}
 */
private String makeSessionKey(String userId, String sessionId) {
    String uid = userId != null ? userId : "anonymous";
    return uid + ":" + sessionId;
}

/**
 * 内部存储键 (向后兼容): {slug}:{threadId}
 */
private String makeStorageKey(String threadId) {
    return tenantPrefix.replace("/", "-") + ":" + threadId;
}
```

---

### 3.4 `/config` 权限要求苛刻 (缺陷 2.4)

**修复方案：** `Dockerfile` 改进

```dockerfile
FROM eclipse-temurin:21-jre
WORKDIR /app

# 创建应用用户
RUN groupadd -r appuser && useradd -r -g appuser -u 1001 appuser

# 预创建配置目录并设置权限
RUN mkdir -p /config/.agentscope/workspace && chown -R appuser:appuser /config

COPY --from=builder /app/target/agent-framework-*.jar app.jar
USER appuser

EXPOSE 8100
ENTRYPOINT ["java", "-jar", "app.jar"]
```

**启动脚本权限检查：**

```bash
#!/bin/bash
# entrypoint.sh
if ! touch /config/.permission_check 2>/dev/null; then
    echo "ERROR: /config not writable by $(id -u)"
    echo "Fix: docker run -v /host/config:/config --user 1001:1001 ..."
    exit 1
fi
rm -f /config/.permission_check
exec java -jar app.jar "$@"
```

---

### 3.5 `GET /` 与 `/health` 信息分散 (缺陷 2.5)

**修复方案：** `InfoController.java` 新增 `/metadata` 端点

```java
@GetMapping("/metadata")
public Map<String, Object> getMetadata(
    @RequestParam(defaultValue = "false") boolean includeDetails
) {
    var result = new java.util.LinkedHashMap<String, Object>();
    
    result.put("name", oafConfig.name());
    result.put("slug", oafConfig.slug());
    result.put("version", oafConfig.version());
    result.put("description", oafConfig.description());
    
    result.put("skills", oafConfig.skills().stream()
        .map(s -> Map.of("name", s.name(), "description", s.description() != null ? s.description() : ""))
        .toList());
    result.put("mcp", mcpManager.getMcpSummaries(mcpConfigs));
    result.put("protocols", Map.of("a2a", "1.0.0", "a2ui", "v0.8", "oaf", "v0.8.0"));
    
    if (includeDetails) {
        result.put("tools", oafConfig.tools());
        result.put("subAgents", oafConfig.subAgents());
        result.put("model", oafConfig.model());
    }
    
    return result;
}

// 保持兼容
@GetMapping("/")
public Map<String, Object> root() {
    return Map.of(
        "agent", oafConfig.name(),
        "slug", oafConfig.slug(),
        "version", oafConfig.version(),
        "skills", oafConfig.skills().size(),
        "mcp", oafConfig.mcpServers().size(),
        "metadata_url", "/metadata?includeDetails=true"
    );
}
```

---

## 4. P3 级缺陷修复方案

### 4.1 skills 模型僵硬 (缺陷 3.1)

**问题代码：** `OafConfigLoader.java:107,119`

```java
skills.add(new SkillConfig(..., List.of("bash", "python")));  // 硬编码 runtimes
return text.length() > 500 ? text.substring(0, 500) : text;  // 硬截断
```

**修复方案：** `OafConfigLoader.java`

```java
private List<SkillConfig> parseSkills(Map<String, Object> fm) {
    var raw = (List<Map<String, Object>>) fm.getOrDefault("skills", Collections.emptyList());
    if (raw == null) return Collections.emptyList();
    
    return raw.stream().map(m -> {
        var name = (String) m.getOrDefault("name", "");
        var source = (String) m.getOrDefault("source", "local");
        var version = (String) m.getOrDefault("version", "1.0.0");
        var required = (boolean) m.getOrDefault("required", false);
        var desc = loadSkillDescription(name);
        
        // 修复: 支持自定义 runtimes
        List<String> runtimes = parseRuntimes(m);
        
        return new SkillConfig(name, source, version, required, desc, runtimes);
    }).toList();
}

@SuppressWarnings("unchecked")
private List<String> parseRuntimes(Map<String, Object> m) {
    var raw = m.get("runtimes");
    if (raw instanceof List<?> list) {
        return list.stream().filter(String.class::isInstance).map(String.class::cast).toList();
    }
    return List.of("bash", "python");  // 默认值
}

private String loadSkillDescription(String skillName) {
    var skillMd = configDir.resolve("skills").resolve(skillName).resolve("SKILL.md");
    if (Files.exists(skillMd)) {
        try {
            var content = Files.readString(skillMd);
            var parsed = parseFrontmatter(content);
            var text = parsed.body();
            // 修复: 不再硬截断
            if (text.length() > 2000) {
                log.warn("Skill '{}' description is long ({} chars)", skillName, text.length());
            }
            return text;
        } catch (IOException e) {
            return "";
        }
    }
    return "";
}
```

---

### 4.2 MCP 工具能力不可枚举 (缺陷 3.2)

**修复方案：** `ToolController.java` 新增 `/mcp/tools` 端点

```java
@GetMapping("/mcp/tools")
public List<Map<String, Object>> listMcpTools(
    @RequestParam(required = false) String server
) {
    if (server != null && !server.isBlank()) {
        return mcpToolRegistrar.enumerateTools(server);
    }
    
    return mcpConfigs.stream()
        .map(c -> (String) c.get("server"))
        .filter(Objects::nonNull)
        .flatMap(s -> mcpToolRegistrar.enumerateTools(s).stream())
        .toList();
}
```

---

### 4.3 DB 初始化契约不清 (缺陷 3.3)

**修复方案：** 新增 `DatabaseHealthChecker.java`

```java
@Component
public class DatabaseHealthChecker {
    private static final Logger log = LoggerFactory.getLogger(DatabaseHealthChecker.class);
    private final JdbcTemplate jdbcTemplate;
    
    public DatabaseHealthChecker(JdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
    }
    
    @EventListener(ApplicationReadyEvent.class)
    public void check() {
        log.info("Checking database...");
        try {
            jdbcTemplate.queryForObject("SELECT 1", Integer.class);
            log.info("Database: OK");
        } catch (Exception e) {
            log.error("Database check failed: {}", e.getMessage());
            log.error("Required: CREATE DATABASE IF NOT EXISTS agent_manager_test;");
            throw new IllegalStateException("Database not ready", e);
        }
    }
}
```

---

## 5. 修改文件清单

| 文件 | 修改内容 | 优先级 |
|------|---------|--------|
| `A2AController.java` | 修复 A2A 参数解析 | P1 |
| `AgentRuntimeService.java` | 集成 SessionManager | P1 |
| `SessionManager.java` | **新增** 会话管理 | P1 |
| `SessionCleanupService.java` | **新增** 定时清理 | P1 |
| `ToolController.java` | 修复 /tools, 新增 /mcp/tools | P1 |
| `McpManager.java` | 修复 ActiveMCP.json 解析 | P1 |
| `McpToolRegistrar.java` | 添加工具查询方法 | P1 |
| `InfoController.java` | 新增 /metadata 端点 | P2 |
| `OafConfigLoader.java` | 修复 skills 解析 | P3 |
| `DatabaseHealthChecker.java` | **新增** 启动检查 | P3 |
| `Dockerfile` | 权限处理 | P2 |
| `AgentFrameworkApplication.java` | 启用 @EnableScheduling | P1 |

---

## 6. 实施路线图

### Phase 1: 核心修复 (3-5 天)

| 任务 | 工作量 | 依赖 |
|------|--------|------|
| A2AController 参数解析 | 1d | 无 |
| SessionManager | 1d | 无 |
| McpManager ActiveMCP 修复 | 0.5d | 无 |
| ToolController /tools 修复 | 0.5d | 无 |
| 单元测试 | 1d | 以上 |

### Phase 2: 功能增强 (3-5 天)

| 任务 | 工作量 | 依赖 |
|------|--------|------|
| SessionCleanupService | 0.5d | SessionManager |
| /metadata 端点 | 0.5d | 无 |
| /mcp/tools 端点 | 1d | McpToolRegistrar |
| 权限检查优化 | 0.5d | 无 |
| 集成测试 | 1d | 以上 |

### Phase 3: 体验优化 (2-3 天)

| 任务 | 工作量 | 依赖 |
|------|--------|------|
| Skills 模型增强 | 0.5d | 无 |
| DB 启动检查 | 0.5d | 无 |
| 文档更新 | 1d | 以上 |
| 回归测试 | 0.5d | 以上 |

---

## 7. 验证方案

### 7.1 手动验证

```bash
# 1. 标准 A2A 参数
curl -X POST http://localhost:8100/ \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","method":"message/send","params":{"userId":"u1","sessionId":"s1","message":{"role":"user","parts":[{"text":"hello"}]}},"id":"1"}'

# 2. /tools 端点
curl http://localhost:8100/tools

# 3. /mcp 端点 (放置 ActiveMCP.json 后)
curl http://localhost:8100/mcp

# 4. /metadata 端点
curl http://localhost:8100/metadata?includeDetails=true

# 5. 会话续接
# 发送两次相同 userId+sessionId，验证复用同一会话
```

### 7.2 自动化测试

```bash
cd agent-framework
mvn test
```

---

## 8. 验证状态对照

| 验证项 | 原状态 | 修复后预期 |
|--------|--------|-----------|
| V1 `/config` 挂载读 | ✅ | ✅ |
| V2 skills/ 生效 | ✅ | ✅ |
| V3 mcp-configs 生效 | ✅ | ✅ |
| V4 LLM 变量生效 | ✅ | ✅ |
| V5 ActiveMCP 工具子集 | ❌ | ✅ |

## 9. Debug 页面适配

Debug 页面需要同步修改以适配新的 API 返回格式。

### 9.1 api.js 修改

**文件：** `src/main/resources/static/debug/js/api.js`

```javascript
export const api = {
  // ... 其他不变 ...

  // 工具 / 技能 / MCP
  getTools: (includeInternal = true) => get('/tools?includeInternal=' + includeInternal),
  getSkills: () => get('/skills'),
  getMcpServers: () => get('/mcp'),
  
  // 新增: MCP 工具枚举
  getMcpTools: (server) => get('/mcp/tools' + (server ? '?server=' + server : '')),

  // A2A 调用 - 支持顶层参数
  sendA2A: (text, { userId, sessionId, metadata } = {}) => {
    const params = {
      message: { role: 'user', parts: [{ text }] }
    };
    // 优先使用顶层参数
    if (userId) params.userId = userId;
    if (sessionId) params.sessionId = sessionId;
    // 兼容旧格式 metadata
    if (metadata) params.metadata = metadata;
    
    return post('/', {
      jsonrpc: '2.0',
      method: 'message/send',
      id: 'debug-' + Date.now(),
      params
    });
  }
};
```

### 9.2 tools.js 修改

**文件：** `src/main/resources/static/debug/modules/tools.js`

```javascript
async function loadTools() {
  const body = document.getElementById('toolsBody');
  let data;
  try {
    data = await ctx.api.getTools(true);  // 包含内置工具
  } catch (e) {
    body.innerHTML = '<div class="empty error-text">Failed to load tools: ' + ctx.utils.esc(e.message) + '</div>';
    return;
  }
  
  const tools = data.tools || [];
  const mcpTools = tools.filter(t => t.category === 'mcp');
  const internalTools = tools.filter(t => t.category === 'internal');
  
  document.getElementById('toolsSummary').textContent =
    tools.length + ' total (' + internalTools.length + ' internal / ' + mcpTools.length + ' mcp)';

  let html = '';
  
  // MCP 工具（按 server 分组）
  if (mcpTools.length > 0) {
    const grouped = {};
    mcpTools.forEach(t => {
      const server = t.server || 'unknown';
      if (!grouped[server]) grouped[server] = [];
      grouped[server].push(t);
    });
    
    html += '<div class="panel"><div class="panel-header"><span class="title">MCP Tools</span><span class="count">' + mcpTools.length + '</span></div><div class="panel-body">';
    for (const [server, tools] of Object.entries(grouped)) {
      html += '<div style="margin:6px 0"><strong style="color:var(--accent)">🔌 ' + ctx.utils.esc(server) + '</strong>' +
        '<span class="badge dim" style="margin-left:6px">' + tools.length + ' tools</span></div>';
      html += '<div style="padding-left:12px">' +
        tools.map(t => '<span class="badge" title="' + ctx.utils.esc(t.description || '') + '">' + ctx.utils.esc(t.name) + '</span>').join('') +
        '</div>';
    }
    html += '</div></div>';
  }
  
  // 内置工具
  if (internalTools.length > 0) {
    html += renderSection('Built-in Tools', internalTools.length, internalTools.map(t => ({
      name: t.name, badge: ''
    })));
  }

  body.innerHTML = html || '<div class="empty">No tools</div>';
}
```

### 9.3 chat.js 修改（可选）

如果 chat 模块支持指定 userId/sessionId，可适配顶层参数：

```javascript
// 发送消息时支持 userId/sessionId
async function sendMessage(text, { userId, sessionId } = {}) {
  return ctx.api.sendA2A(text, { userId, sessionId });
}
```

### 9.4 修改清单

| 文件 | 修改内容 | 必要性 |
|------|---------|--------|
| `api.js` | 更新 `getTools` 参数、`sendA2A` 支持顶层参数 | **必须** |
| `tools.js` | 适配新的 `/tools` 返回格式 | **必须** |
| `mcp.js` | 无需修改 | ✅ |
| `chat.js` | 可选支持 userId/sessionId 参数 | 建议 |

## 10. agent-framework/docs 文档同步更新

修复完成后，`agent-framework/docs/` 目录下的文档需要同步更新。

### 10.1 文档修改清单

| 文档 | 状态 | 修改内容 |
|------|------|---------|
| `api.md` | ⚠️ **必须改** | `/tools` 响应格式、新增 `/metadata`、`message/send` 参数 |
| `a2a-improvement-plan.md` | ⚠️ **建议改** | 补充缺陷 1.1/1.2 修复说明 |
| `debug-page-refactor-plan.md` | ⚠️ **建议改** | 更新 tools.js/api.js 变更说明 |
| `agent-framework-design.md` | 💡 可选 | 架构图新增 SessionManager |
| `tool-system-improvement-plan.md` | ✅ 无需改 | 已是改进方案 |

### 10.2 api.md 修改内容

**文件：** `agent-framework/docs/api.md`

#### 10.2.1 更新 `/tools` 端点响应格式

```markdown
### GET /tools

工具列表（支持分类查询）。

```bash
curl http://localhost:8100/tools
curl http://localhost:8100/tools?includeInternal=true
```

**参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `includeInternal` | boolean | | 是否包含内置工具（默认 false） |

**响应：**

```json
{
    "tools": [
        {
            "name": "get_weather",
            "server": "weather-service",
            "category": "mcp",
            "description": "获取天气信息"
        },
        {
            "name": "read_file",
            "category": "internal",
            "description": "读取文件内容"
        }
    ],
    "totalCount": 22,
    "mcpCount": 5
}
```
```

#### 10.2.2 新增 `/metadata` 端点

```markdown
### GET /metadata

完整 Agent 元数据端点。

```bash
curl http://localhost:8100/metadata
curl http://localhost:8100/metadata?includeDetails=true
```

**参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `includeDetails` | boolean | | 是否包含详细信息（默认 false） |

**响应：**

```json
{
    "name": "test-agent",
    "slug": "acme/test-agent",
    "version": "1.0.0",
    "description": "A test agent",
    "skills": [
        {"name": "code-review", "description": "Code review skill"}
    ],
    "mcp": [
        {"server": "weather", "tool_count": 3, "connection_type": "sse"}
    ],
    "protocols": {"a2a": "1.0.0", "a2ui": "v0.8", "oaf": "v0.8.0"}
}
```
```

#### 10.2.3 更新 `message/send` 参数说明

```markdown
### message/send

发送同步消息，返回完整 Agent 响应。

**请求参数：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `params.message.role` | string | ✓ | `"user"` 或 `"agent"` |
| `params.message.parts` | array | ✓ | Part 数组 |
| `params.userId` | string | | 用户标识（标准 A2A 顶层参数） |
| `params.sessionId` | string | | 会话标识（标准 A2A 顶层参数） |
| `params.conversationId` | string | | 对话标识（标准 A2A 顶层参数） |
| `params.metadata.userId` | string | | 用户标识（旧格式，向后兼容） |
| `params.metadata.thread_id` | string | | 线程标识（旧格式，向后兼容） |

> **参数优先级**: 顶层参数 > metadata 参数 > 自动生成

**请求示例（标准 A2A 格式）：**

```json
{
    "jsonrpc": "2.0",
    "method": "message/send",
    "params": {
        "userId": "user-123",
        "sessionId": "session-456",
        "message": {
            "role": "user",
            "parts": [{"kind": "text", "text": "你好"}]
        }
    },
    "id": "1"
}
```

**请求示例（旧格式，向后兼容）：**

```json
{
    "jsonrpc": "2.0",
    "method": "message/send",
    "params": {
        "metadata": {
            "userId": "user-123",
            "thread_id": "thread-789"
        },
        "message": {
            "role": "user",
            "parts": [{"kind": "text", "text": "你好"}]
        }
    },
    "id": "1"
}
```
```

### 10.3 a2a-improvement-plan.md 修改内容

**文件：** `agent-framework/docs/a2a-improvement-plan.md`

在 **1.2 关键协议差异** 章节后新增：

```markdown
#### (e) 参数解析不兼容 (缺陷 1.1)

**问题代码：** `A2AController.java:171-203`

```java
// 当前实现 - 只读 metadata
private String resolveUserId(Map<String, Object> params) {
    var metadata = (Map<String, Object>) params.get("metadata");
    if (metadata != null) {
        var uid = metadata.get("userId");  // 只看 metadata.userId
        // ...
    }
    return null;  // 忽略顶层 userId
}
```

**A2A 1.0 规范要求：**
```json
{
  "params": {
    "userId": "user-123",           // 顶层参数（优先）
    "sessionId": "session-456",    // 顶层参数（优先）
    "metadata": {
      "userId": "...",             // 旧格式（兼容）
      "thread_id": "..."          // 旧格式（兼容）
    }
  }
}
```

**修复方案：** 顶层参数优先读取，兼容旧格式 metadata

#### (f) 会话续接缺陷 (缺陷 1.2)

**问题代码：** `AgentRuntimeService.java:90-92`

```java
if (threadId == null || threadId.isEmpty()) {
    threadId = UUID.randomUUID().toString();  // 每次生成新 UUID，无法续接
}
```

**修复方案：** 新增 `SessionManager` 管理会话生命周期

```java
@Service
public class SessionManager {
    private static final Duration DEFAULT_TTL = Duration.ofDays(30);
    
    public SessionState getOrCreateSession(String userId, String sessionId) {
        // 根据 userId + sessionId 查找或创建会话
        // 支持会话续接，TTL 30 天
    }
}
```
```

### 10.4 debug-page-refactor-plan.md 修改内容

**文件：** `agent-framework/docs/debug-page-refactor-plan.md`

在文档末尾的 **更新记录** 章节新增：

```markdown
- **2026-08-07**: 适配后端 API 变更
  - `api.js`: `getTools()` 新增 `includeInternal` 参数
  - `api.js`: `sendA2A()` 支持顶层 userId/sessionId 参数
  - `tools.js`: 适配新的 `/tools` 响应格式 `{tools, totalCount, mcpCount}`
```

### 10.5 agent-framework-design.md 修改内容（可选）

**文件：** `agent-framework/docs/agent-framework-design.md`

在 **2.2 模块分层** 的 service 层新增：

```markdown
│   ├── SessionManager.java          # 会话管理 (TTL 30天，自动清理)
│   ├── SessionCleanupService.java   # 会话定时清理 (每天凌晨3点)
```

---

*文档版本: v1.1 | 最后更新: 2026-08-07*