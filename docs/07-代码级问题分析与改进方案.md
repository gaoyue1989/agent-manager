# Agent Framework 代码级问题分析与改进方案

版本: v1.0
日期: 2026-08-07
基于: agent-framework 源码分析

---

## 1. P1 级缺陷代码定位与修复

### 1.1 A2A 协议参数解析不兼容

**问题代码位置：** `A2AController.java:171-203`

```java
// 当前实现 - 只读取 metadata 中的参数
private String resolveUserId(Map<String, Object> params) {
    var metadata = (Map<String, Object>) params.get("metadata");  // 行 172
    if (metadata != null) {
        var uid = metadata.get("userId");  // 行 174 - 只看 metadata.userId
        if (uid != null && !uid.toString().isBlank()) {
            return uid.toString();
        }
    }
    return null;  // 行 179 - 忽略顶层 userId
}

private String resolveThreadId(Map<String, Object> params, Map<String, Object> message) {
    var metadata = (Map<String, Object>) params.get("metadata");
    if (metadata != null) {
        var tid = metadata.get("thread_id");      // 行 188 - 只看 metadata.thread_id
        var ctxId = metadata.get("contextId");    // 行 192 - 只看 metadata.contextId
    }
    // 忽略顶层 sessionId / conversationId
}
```

**A2A 1.0 规范要求：**
```json
{
  "params": {
    "message": {...},
    "userId": "user-123",           // 顶层参数
    "sessionId": "session-456",    // 顶层参数
    "conversationId": "conv-789"   // 顶层参数
  }
}
```

**修复方案：** `A2AController.java`

```java
@SuppressWarnings("unchecked")
private String resolveUserId(Map<String, Object> params) {
    // 1. 优先读取标准 A2A 顶层参数
    Object topLevelUserId = params.get("userId");
    if (topLevelUserId != null && !topLevelUserId.toString().isBlank()) {
        return topLevelUserId.toString();
    }
    
    // 2. 兼容旧格式 metadata.userId
    var metadata = (Map<String, Object>) params.get("metadata");
    if (metadata != null) {
        var uid = metadata.get("userId");
        if (uid != null && !uid.toString().isBlank()) {
            log.warn("Deprecated: using metadata.userId, migrate to top-level userId");
            return uid.toString();
        }
    }
    
    return null; // 回退到默认 vendorKey
}

@SuppressWarnings("unchecked")
private String resolveThreadId(Map<String, Object> params, Map<String, Object> message) {
    // 1. 优先读取标准 A2A 顶层参数
    Object sessionId = params.get("sessionId");
    if (sessionId != null && !sessionId.toString().isBlank()) {
        return sessionId.toString();
    }
    
    Object conversationId = params.get("conversationId");
    if (conversationId != null && !conversationId.toString().isBlank()) {
        return conversationId.toString();
    }
    
    // 2. 兼容旧格式 metadata
    var metadata = (Map<String, Object>) params.get("metadata");
    if (metadata != null) {
        var tid = metadata.get("thread_id");
        if (tid != null && !tid.toString().isBlank()) {
            log.warn("Deprecated: using metadata.thread_id, migrate to top-level sessionId");
            return tid.toString();
        }
        var ctxId = metadata.get("contextId");
        if (ctxId != null && !ctxId.toString().isBlank()) {
            return ctxId.toString();
        }
    }
    
    // 3. message.taskId (兼容)
    var taskId = message.get("taskId");
    if (taskId != null && !taskId.toString().isBlank()) {
        return taskId.toString();
    }
    
    // 4. 自动生成
    return UUID.randomUUID().toString();
}
```

**验收测试：**
```java
@Test
void testStandardA2AParams() {
    var params = Map.<String, Object>of(
        "userId", "user-123",
        "sessionId", "session-456",
        "message", Map.of("role", "user", "parts", List.of(Map.of("text", "hello")))
    );
    
    var result = controller.handleMessageSend("req-1", Map.of("params", params, "method", "message/send"));
    // 验证 userId=user-123, threadId=session-456
}

@Test
void testBackwardCompatibility() {
    var params = Map.<String, Object>of(
        "metadata", Map.of("userId", "user-123", "thread_id", "thread-789"),
        "message", Map.of("role", "user", "parts", List.of(Map.of("text", "hello")))
    );
    
    var result = controller.handleMessageSend("req-1", Map.of("params", params, "method", "message/send"));
    // 验证 userId=user-123, threadId=thread-789 (使用旧格式)
}
```

---

### 1.2 会话续接缺陷

**问题代码位置：** `AgentRuntimeService.java:90-92`

```java
public Map<String, Object> invoke(String message, String threadId, String userId) {
    if (threadId == null || threadId.isEmpty()) {
        threadId = UUID.randomUUID().toString();  // 行 91 - 每次生成新 UUID
    }
    // ... 每次请求都是独立会话，无法续接
}
```

**问题分析：**
- 同一 userId + sessionId 的多次请求，如果没有显式传递 threadId，会生成不同的 UUID
- 缺乏会话状态查询机制，无法复用已有会话

**修复方案：** 新增 `SessionManager.java`

```java
package io.agentmanager.framework.service;

import java.time.Duration;
import java.time.Instant;
import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

@Service
public class SessionManager {
    private static final Logger log = LoggerFactory.getLogger(SessionManager.class);
    private static final Duration DEFAULT_TTL = Duration.ofMinutes(30);
    
    // 会话缓存: sessionKey -> SessionState
    private final Map<String, SessionState> sessions = new ConcurrentHashMap<>();
    
    public record SessionState(
        String sessionKey,
        String userId,
        String threadId,
        Instant createdAt,
        Instant expiresAt
    ) {
        public boolean isExpired() {
            return Instant.now().isAfter(expiresAt);
        }
    }
    
    /**
     * 获取或创建会话
     * @param userId 用户 ID
     * @param sessionId 会话 ID (来自 A2A sessionId/conversationId)
     * @return 会话状态
     */
    public SessionState getOrCreateSession(String userId, String sessionId) {
        String sessionKey = buildSessionKey(userId, sessionId);
        
        // 1. 尝试获取已有会话
        SessionState existing = sessions.get(sessionKey);
        if (existing != null && !existing.isExpired()) {
            log.debug("Resuming session: {}", sessionKey);
            return existing;
        }
        
        // 2. 创建新会话
        SessionState newSession = new SessionState(
            sessionKey,
            userId,
            sessionId,
            Instant.now(),
            Instant.now().plus(DEFAULT_TTL)
        );
        
        sessions.put(sessionKey, newSession);
        log.info("Created new session: {}", sessionKey);
        return newSession;
    }
    
    /**
     * 根据 userId + sessionId 查找现有会话的 threadId
     */
    public String resolveThreadId(String userId, String sessionId) {
        if (sessionId == null || sessionId.isBlank()) {
            return null;
        }
        
        String sessionKey = buildSessionKey(userId, sessionId);
        SessionState state = sessions.get(sessionKey);
        
        if (state != null && !state.isExpired()) {
            return state.threadId();
        }
        
        return null;
    }
    
    private String buildSessionKey(String userId, String sessionId) {
        String uid = userId != null ? userId : "anonymous";
        return uid + ":" + sessionId;
    }
    
    /**
     * 清理过期会话
     */
    public int cleanupExpiredSessions() {
        Instant now = Instant.now();
        int count = 0;
        
        var iterator = sessions.entrySet().iterator();
        while (iterator.hasNext()) {
            var entry = iterator.next();
            if (entry.getValue().isExpired()) {
                iterator.remove();
                count++;
            }
        }
        
        if (count > 0) {
            log.info("Cleaned up {} expired sessions", count);
        }
        return count;
    }
    
    public int getActiveSessionCount() {
        return (int) sessions.values().stream()
            .filter(s -> !s.isExpired())
            .count();
    }
}
```

**修改 `AgentRuntimeService.java`：**

```java
public class AgentRuntimeService {
    // ... 现有字段 ...
    private final SessionManager sessionManager;
    
    public AgentRuntimeService(
        OafConfig oafConfig,
        HarnessAgent agent,
        List<Map<String, Object>> mcpConfigs,
        LLMLogger llmLogger,
        SessionManager sessionManager  // 新增
    ) {
        // ... 现有初始化 ...
        this.sessionManager = sessionManager;
    }
    
    public Map<String, Object> invoke(String message, String threadId, String userId) {
        var resolvedUserId = resolveUserId(userId);
        
        // 1. 如果没有 threadId，尝试从 SessionManager 解析
        if (threadId == null || threadId.isEmpty()) {
            // 这里需要 sessionId 参数，需要修改 A2AController 传递
            threadId = UUID.randomUUID().toString();
        }
        
        // 2. 通过 SessionManager 管理会话
        var session = sessionManager.getOrCreateSession(resolvedUserId, threadId);
        var fullThreadId = makeThreadId(session.threadId());
        
        // ... 后续逻辑 ...
    }
}
```

---

### 1.3 会话无回收机制

**问题分析：**
- 当前实现中，会话状态只存在于内存中（AgentRuntimeService 无状态）
- 但 `agent_state` 表和 `agent_fs` 表中的记录没有 TTL 机制
- 每次请求如果 threadId 不同，都会在数据库中创建新记录

**修复方案：** 新增定时清理任务

```java
package io.agentmanager.framework.service;

import java.time.Instant;
import java.time.temporal.ChronoUnit;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Service;

@Service
public class SessionCleanupService {
    private static final Logger log = LoggerFactory.getLogger(SessionCleanupService.class);
    
    private final JdbcTemplate jdbcTemplate;
    private final SessionManager sessionManager;
    
    // 默认保留 7 天的会话数据
    private static final int RETENTION_DAYS = 7;
    
    public SessionCleanupService(JdbcTemplate jdbcTemplate, SessionManager sessionManager) {
        this.jdbcTemplate = jdbcTemplate;
        this.sessionManager = sessionManager;
    }
    
    /**
     * 每小时清理一次过期会话
     */
    @Scheduled(fixedDelay = 3600000) // 1 小时
    public void cleanupExpiredSessions() {
        log.info("Starting session cleanup...");
        
        // 1. 清理内存中的过期会话
        int memoryCleaned = sessionManager.cleanupExpiredSessions();
        
        // 2. 清理数据库中的过期记录
        Instant cutoff = Instant.now().minus(RETENTION_DAYS, ChronoUnit.DAYS);
        
        int stateCleaned = jdbcTemplate.update(
            "DELETE FROM agent_state WHERE updated_at < ?",
            cutoff
        );
        
        int fsCleaned = jdbcTemplate.update(
            "DELETE FROM agent_fs WHERE updated_at < ?",
            cutoff
        );
        
        log.info("Session cleanup completed: memory={}, agent_state={}, agent_fs={}",
            memoryCleaned, stateCleaned, fsCleaned);
    }
}
```

**启用定时任务：** 在 `AgentFrameworkApplication.java` 添加

```java
@SpringBootApplication
@EnableScheduling  // 新增
public class AgentFrameworkApplication {
    public static void main(String[] args) {
        SpringApplication.run(AgentFrameworkApplication.class, args);
    }
}
```

---

### 1.4 `/tools` 端点硬编码返回空

**问题代码位置：** `ToolController.java:50-57`

```java
@GetMapping("/tools")
public Map<String, Object> listTools() {
    return Map.of(
        "builtin", oafConfig.tools(),
        "custom", List.of(),
        "mcp", List.of()  // 硬编码空列表
    );
}
```

**修复方案：** `ToolController.java`

```java
@RestController
public class ToolController {

    private final OafConfig oafConfig;
    private final AgentRuntimeService agentRuntime;
    private final List<Map<String, Object>> mcpConfigs;
    private final McpManager mcpManager;
    private final McpToolRegistrar mcpToolRegistrar;  // 新增

    public ToolController(
        OafConfig oafConfig,
        AgentRuntimeService agentRuntime,
        List<Map<String, Object>> mcpConfigs,
        McpManager mcpManager,
        McpToolRegistrar mcpToolRegistrar  // 新增
    ) {
        this.oafConfig = oafConfig;
        this.agentRuntime = agentRuntime;
        this.mcpConfigs = mcpConfigs;
        this.mcpManager = mcpManager;
        this.mcpToolRegistrar = mcpToolRegistrar;
    }

    @GetMapping("/tools")
    public Map<String, Object> listTools(
        @RequestParam(defaultValue = "false") boolean includeInternal
    ) {
        List<Map<String, Object>> tools = new ArrayList<>();
        
        // 1. MCP 工具
        List<Map<String, Object>> mcpTools = getMcpTools();
        tools.addAll(mcpTools);
        
        // 2. 内置工具（可选）
        if (includeInternal) {
            List<Map<String, Object>> internalTools = oafConfig.tools().stream()
                .map(name -> Map.<String, Object>of(
                    "name", name,
                    "category", "internal",
                    "description", ""
                ))
                .toList();
            tools.addAll(internalTools);
        }
        
        return Map.of(
            "tools", tools,
            "totalCount", tools.size(),
            "mcpCount", mcpTools.size()
        );
    }
    
    private List<Map<String, Object>> getMcpTools() {
        List<Map<String, Object>> result = new ArrayList<>();
        
        for (var config : mcpConfigs) {
            String serverName = (String) config.getOrDefault("server", "unknown");
            
            // 从 McpToolRegistrar 获取已注册的工具
            var registeredTools = mcpToolRegistrar.getToolsByServer(serverName);
            for (var tool : registeredTools) {
                result.add(Map.<String, Object>of(
                    "name", tool.getName(),
                    "server", serverName,
                    "category", "mcp",
                    "description", tool.getDescription() != null ? tool.getDescription() : ""
                ));
            }
        }
        
        return result;
    }
    
    @GetMapping("/mcp")
    public List<Map<String, Object>> listMcp() {
        return mcpManager.getMcpSummaries(mcpConfigs);
    }
    
    @GetMapping("/skills")
    public List<Map<String, Object>> listSkills() {
        return oafConfig.skills().stream()
            .map(s -> Map.<String, Object>of(
                "name", s.name(),
                "description", s.description() != null ? s.description() : "",
                "version", s.version(),
                "source", s.source()
            ))
            .toList();
    }
}
```

**修改 `McpToolRegistrar.java` 添加工具查询方法：**

```java
@Service
public class McpToolRegistrar {
    // ... 现有代码 ...
    
    // 新增：按 server 名称查询已注册工具
    public List<ToolInfo> getToolsByServer(String serverName) {
        return registeredTools.entrySet().stream()
            .filter(entry -> entry.getKey().startsWith(serverName + ":"))
            .map(Map.Entry::getValue)
            .collect(Collectors.toList());
    }
    
    // 新增：获取所有已注册工具
    public List<ToolInfo> getAllRegisteredTools() {
        return new ArrayList<>(registeredTools.values());
    }
    
    public record ToolInfo(String name, String description, String serverName) {}
}
```

---

### 1.5 ActiveMCP.json 导致 `/mcp` 500 错误

**问题代码位置：** `McpManager.java:51-59, 77-93`

```java
// 行 51-59: 加载 ActiveMCP.json
var activeMcp = mcpDir.resolve("ActiveMCP.json");
if (activeMcp.toFile().exists()) {
    try {
        var mapper = new com.fasterxml.jackson.databind.ObjectMapper();
        config.put("tools", mapper.readTree(activeMcp.toFile()));  // 行 55: 返回 JsonNode
    } catch (Exception e) {
        log.warn("Failed to load ActiveMCP.json for {}: {}", ms.server(), e.getMessage());
    }
}

// 行 77-93: 获取摘要时强转失败
public List<Map<String, Object>> getMcpSummaries(List<Map<String, Object>> mcpConfigs) {
    for (var mc : mcpConfigs) {
        var tools = (Map<String, Object>) mc.getOrDefault("tools", Map.of());  // 行 83: ClassCastException!
        var toolCount = ((List<?>) tools.getOrDefault("selectedTools", List.of())).size();  // 行 89
    }
}
```

**问题分析：**
- `mapper.readTree()` 返回 `JsonNode` (具体是 `ObjectNode`)
- 行 83 尝试将 `JsonNode` 强转为 `Map<String, Object>`，必然抛出 `ClassCastException`

**修复方案：** `McpManager.java`

```java
@Service
public class McpManager {
    private static final Logger log = LoggerFactory.getLogger(McpManager.class);

    private final Path mcpConfigsDir;
    private final com.fasterxml.jackson.databind.ObjectMapper mapper;

    public McpManager(java.nio.file.Path configDir) {
        this.mcpConfigsDir = configDir;
        this.mapper = new com.fasterxml.jackson.databind.ObjectMapper();
    }

    @SuppressWarnings("unchecked")
    private Map<String, Object> loadSingleConfig(McpServerConfig ms) {
        var mcpDir = mcpConfigsDir.resolve(ms.configDir().isEmpty() ? ms.server() : ms.configDir());
        if (!mcpDir.toFile().exists()) {
            mcpDir = mcpConfigsDir.resolve(ms.server());
        }
        if (!mcpDir.toFile().exists()) {
            return null;
        }

        var config = new java.util.LinkedHashMap<String, Object>();
        config.put("vendor", ms.vendor());
        config.put("server", ms.server());
        config.put("version", ms.version());
        config.put("required", ms.required());

        var activeMcp = mcpDir.resolve("ActiveMCP.json");
        if (activeMcp.toFile().exists()) {
            try {
                var jsonNode = mapper.readTree(activeMcp.toFile());
                // 修复: 安全转换 JsonNode 为 Map
                Map<String, Object> toolsMap = safeJsonNodeToMap(jsonNode);
                config.put("tools", toolsMap);
            } catch (Exception e) {
                log.warn("Failed to load ActiveMCP.json for {}: {}", ms.server(), e.getMessage());
                config.put("tools", Map.of());  // 使用空 Map 作为默认值
            }
        }

        var configYaml = mcpDir.resolve("config.yaml");
        if (configYaml.toFile().exists()) {
            try {
                var yaml = new org.yaml.snakeyaml.Yaml();
                var yamlData = (Map<String, Object>) yaml.load(configYaml.toFile().toURI().toURL().openStream());
                if (yamlData != null && yamlData.containsKey("connection")) {
                    config.put("connection", yamlData.get("connection"));
                }
            } catch (Exception e) {
                log.warn("Failed to load config.yaml for {}: {}", ms.server(), e.getMessage());
            }
        }

        return config;
    }
    
    /**
     * 安全地将 JsonNode 转换为 Map<String, Object>
     */
    @SuppressWarnings("unchecked")
    private Map<String, Object> safeJsonNodeToMap(com.fasterxml.jackson.databind.JsonNode node) {
        if (node == null || node.isNull()) {
            return Map.of();
        }
        
        if (node.isObject()) {
            var result = new java.util.LinkedHashMap<String, Object>();
            node.fields().forEachRemaining(entry -> {
                result.put(entry.getKey(), safeJsonNodeToValue(entry.getValue()));
            });
            return result;
        }
        
        // 如果不是对象类型，包装为 Map
        return Map.of("value", safeJsonNodeToValue(node));
    }
    
    private Object safeJsonNodeToValue(com.fasterxml.jackson.databind.JsonNode node) {
        if (node == null || node.isNull()) {
            return null;
        }
        if (node.isTextual()) {
            return node.asText();
        }
        if (node.isBoolean()) {
            return node.asBoolean();
        }
        if (node.isNumber()) {
            if (node.isInt() || node.isLong()) {
                return node.asLong();
            }
            return node.asDouble();
        }
        if (node.isArray()) {
            var list = new ArrayList<>();
            for (var item : node) {
                list.add(safeJsonNodeToValue(item));
            }
            return list;
        }
        if (node.isObject()) {
            return safeJsonNodeToMap(node);
        }
        return node.toString();
    }

    public List<Map<String, Object>> getMcpSummaries(List<Map<String, Object>> mcpConfigs) {
        var summaries = new ArrayList<Map<String, Object>>();
        for (var mc : mcpConfigs) {
            try {
                @SuppressWarnings("unchecked")
                var conn = (Map<String, Object>) mc.getOrDefault("connection", Map.of());
                
                // 修复: 安全获取 tools 信息
                var toolsObj = mc.get("tools");
                int toolCount = 0;
                if (toolsObj instanceof Map<?, ?> toolsMap) {
                    var selectedTools = toolsMap.get("selectedTools");
                    if (selectedTools instanceof List<?> toolsList) {
                        toolCount = toolsList.size();
                    }
                } else if (toolsObj instanceof com.fasterxml.jackson.databind.JsonNode jsonNode) {
                    // 处理 JsonNode 类型
                    if (jsonNode.has("selectedTools")) {
                        toolCount = jsonNode.get("selectedTools").size();
                    }
                }
                
                summaries.add(Map.of(
                    "server", mc.getOrDefault("server", "unknown"),
                    "vendor", mc.getOrDefault("vendor", ""),
                    "connection_type", conn.getOrDefault("type", "N/A"),
                    "url", conn.getOrDefault("url", "N/A"),
                    "tool_count", toolCount
                ));
            } catch (Exception e) {
                log.warn("Failed to get summary for server: {}", mc.get("server"), e);
                summaries.add(Map.of(
                    "server", mc.getOrDefault("server", "unknown"),
                    "vendor", mc.getOrDefault("vendor", ""),
                    "connection_type", "N/A",
                    "url", "N/A",
                    "tool_count", 0,
                    "error", e.getMessage()
                ));
            }
        }
        return summaries;
    }
}
```

---

## 2. P2 级缺陷代码定位与修复

### 2.1 `/mcp` 的 `tool_count: 0` 失真

**问题代码位置：** `McpManager.java:89`

```java
"tool_count", ((List<?>) tools.getOrDefault("selectedTools", List.of())).size()
```

**问题分析：**
- `tools` 来自 `config.get("tools")`，可能是空 Map 或 JsonNode
- `selectedTools` 字段在 ActiveMCP.json 中可能不存在

**修复方案：** 已在 1.5 中修复，通过安全的类型检查和默认值处理

---

### 2.2 内置工具与业务工具无区分

**问题分析：**
- `OafConfig.tools()` 返回的是 AGENTS.md 中声明的工具列表
- 实际的内置工具（agent_send, echo, plan_* 等）由 HarnessAgent 自动注册
- LLM 请求时会包含所有工具，包括内部工具

**修复方案：** 修改 `AgentRuntimeService.buildSystemPrompt()` 和工具过滤逻辑

```java
// AgentRuntimeService.java

// 内置工具黑名单 - 不暴露给 LLM
private static final Set<String> INTERNAL_TOOL_NAMES = Set.of(
    "agent_send", "agent_spawn", "agent_list",
    "plan_enter", "plan_write", "plan_exit",
    "skill_manage", "load_skill_through_path",
    "task_list", "task_output", "task_cancel", "wait_async_results"
);

public List<String> getToolsForLLM() {
    return oafConfig.tools().stream()
        .filter(name -> !INTERNAL_TOOL_NAMES.contains(name))
        .collect(Collectors.toList());
}
```

---

### 2.3 会话键三端点三种格式

**问题分析：**
- `agent_state.session_id`: `{userId}:{slug}:{thread_id}`
- `agent_fs` item_key: `{slug}:{thread_id}`
- `/chat/stream`: `{userId}:gw-<uuid>`

**修复方案：** 在 `AgentRuntimeService` 中统一格式

```java
// AgentRuntimeService.java

/**
 * 生成统一的会话键
 * 格式: {userId}:{sessionId}
 */
private String makeSessionKey(String userId, String sessionId) {
    String uid = userId != null ? userId : "anonymous";
    return uid + ":" + sessionId;
}

/**
 * 生成内部存储键 (向后兼容)
 * 格式: {slug}:{threadId}
 */
private String makeStorageKey(String threadId) {
    return tenantPrefix.replace("/", "-") + ":" + threadId;
}
```

---

### 2.4 `/config` 权限要求苛刻

**问题代码位置：** `Dockerfile` 和 `WorkspaceInitializer.java`

**修复方案：** 修改 `Dockerfile`

```dockerfile
# Dockerfile
FROM maven:3.9-eclipse-temurin-21 AS builder
# ... 构建阶段 ...

FROM eclipse-temurin:21-jre
WORKDIR /app

# 创建应用用户
RUN groupadd -r appuser && useradd -r -g appuser -u 1001 appuser

# 创建配置目录并设置权限
RUN mkdir -p /config && chown -R appuser:appuser /config

# 复制应用
COPY --from=builder /app/target/agent-framework-*.jar app.jar

# 切换到应用用户
USER appuser

EXPOSE 8100
ENTRYPOINT ["java", "-jar", "app.jar"]
```

**修改启动脚本添加权限检查：**

```bash
#!/bin/bash
# entrypoint.sh

# 检查配置目录权限
if ! touch /config/.permission_check 2>/dev/null; then
    echo "ERROR: /config directory is not writable by user $(id -u)"
    echo "Please run: docker run -v /host/config:/config --user 1001:1001 ..."
    exit 1
fi
rm -f /config/.permission_check

# 启动应用
exec java -jar app.jar "$@"
```

---

### 2.5 `GET /` 与 `/health` 信息分散

**问题代码位置：** `InfoController.java:23-44`

**修复方案：** 新增 `/metadata` 端点

```java
// InfoController.java

@GetMapping("/metadata")
public Map<String, Object> getMetadata(
    @RequestParam(defaultValue = "false") boolean includeDetails
) {
    var builder = new java.util.LinkedHashMap<String, Object>();
    
    // 基本信息
    builder.put("name", oafConfig.name());
    builder.put("slug", oafConfig.slug());
    builder.put("version", oafConfig.version());
    builder.put("description", oafConfig.description());
    
    // 能力信息
    builder.put("skills", oafConfig.skills().stream()
        .map(s -> Map.of("name", s.name(), "description", s.description() != null ? s.description() : ""))
        .toList());
    builder.put("mcp", mcpManager.getMcpSummaries(mcpConfigs));
    
    // 协议信息
    builder.put("protocols", Map.of("a2a", "1.0.0", "a2ui", "v0.8", "oaf", "v0.8.0"));
    
    // 详细信息（可选）
    if (includeDetails) {
        builder.put("tools", oafConfig.tools());
        builder.put("subAgents", oafConfig.subAgents());
        builder.put("model", oafConfig.model());
        builder.put("endpoints", Map.of(
            "agent_card", "/.well-known/agent-card.json",
            "jsonrpc", "/",
            "threads", "/threads",
            "health", "/health",
            "debug", "/debug",
            "metadata", "/metadata"
        ));
    }
    
    return builder;
}

// 保持原有端点兼容
@GetMapping("/")
public Map<String, Object> root() {
    return Map.of(
        "agent", oafConfig.name(),
        "slug", oafConfig.slug(),
        "version", oafConfig.version(),
        "skills", oafConfig.skills().size(),  // 保持数量格式兼容
        "mcp", oafConfig.mcpServers().size(),
        "metadata_url", "/metadata?includeDetails=true"
    );
}

@GetMapping("/health")
public Map<String, Object> health() {
    return Map.of(
        "status", "healthy",
        "agent", oafConfig.name(),
        "slug", oafConfig.slug(),
        "version", oafConfig.version(),
        "checks", Map.of(
            "database", checkDatabase(),
            "mcp", checkMcpServers()
        )
    );
}

private String checkDatabase() {
    try {
        jdbcTemplate.queryForObject("SELECT 1", Integer.class);
        return "ok";
    } catch (Exception e) {
        return "error: " + e.getMessage();
    }
}

private Map<String, Object> checkMcpServers() {
    int total = mcpConfigs.size();
    int active = (int) mcpConfigs.stream()
        .filter(c -> c.containsKey("connection"))
        .count();
    return Map.of("total", total, "active", active);
}
```

---

## 3. P3 级缺陷代码定位与修复

### 3.1 skills 模型僵硬

**问题代码位置：** `OafConfigLoader.java:107`

```java
skills.add(new SkillConfig(name, source, version, required, desc, List.of("bash", "python")));
// 硬编码 runtimes，忽略 SKILL.md 中的声明
```

**问题代码位置：** `OafConfigLoader.java:119`

```java
return text.length() > 500 ? text.substring(0, 500) : text;  // 硬截断 500 字符
```

**修复方案：** `OafConfigLoader.java`

```java
private List<SkillConfig> parseSkills(Map<String, Object> fm) {
    var raw = (List<Map<String, Object>>) fm.getOrDefault("skills", Collections.emptyList());
    if (raw == null) return Collections.emptyList();
    var skills = new ArrayList<SkillConfig>();
    for (var m : raw) {
        var name = (String) m.getOrDefault("name", "");
        var source = (String) m.getOrDefault("source", "local");
        var version = (String) m.getOrDefault("version", "1.0.0");
        var required = (boolean) m.getOrDefault("required", false);
        var desc = loadSkillDescription(name);
        
        // 修复: 支持自定义 runtimes
        List<String> runtimes = parseRuntimes(m);
        
        skills.add(new SkillConfig(name, source, version, required, desc, runtimes));
    }
    return skills;
}

@SuppressWarnings("unchecked")
private List<String> parseRuntimes(Map<String, Object> skillConfig) {
    var rawRuntimes = skillConfig.get("runtimes");
    if (rawRuntimes instanceof List<?> list) {
        return list.stream()
            .filter(String.class::isInstance)
            .map(String.class::cast)
            .collect(Collectors.toList());
    }
    return List.of("bash", "python");  // 默认值
}

private String loadSkillDescription(String skillName) {
    var skillMd = configDir.resolve("skills").resolve(skillName).resolve("SKILL.md");
    if (Files.exists(skillMd)) {
        try {
            var content = Files.readString(skillMd);
            var parsed = parseFrontmatter(content);
            var text = parsed.body();
            // 修复: 不再硬截断，改为软提示
            if (text.length() > 2000) {
                log.warn("Skill '{}' description is very long ({} chars), consider trimming",
                    skillName, text.length());
            }
            return text;
        } catch (IOException e) {
            return "";
        }
    }
    return "";
}
```

---

### 3.2 MCP 工具能力不可枚举

**修复方案：** 在 `McpToolRegistrar` 中添加工具枚举支持

```java
// McpToolRegistrar.java

/**
 * 枚举指定 MCP Server 的所有工具
 */
public List<McpToolInfo> enumerateTools(String serverName) {
    var config = findConfigByServer(serverName);
    if (config == null) {
        return List.of();
    }
    
    try {
        var client = buildMcpClient(config);
        var response = client.sendRequest("tools/list", Map.of());
        
        @SuppressWarnings("unchecked")
        var toolsList = (List<Map<String, Object>>) response.get("tools");
        if (toolsList == null) {
            return List.of();
        }
        
        return toolsList.stream()
            .map(tool -> new McpToolInfo(
                (String) tool.get("name"),
                (String) tool.get("description"),
                (Map<String, Object>) tool.get("inputSchema"),
                serverName
            ))
            .collect(Collectors.toList());
    } catch (Exception e) {
        log.warn("Failed to enumerate tools for server {}: {}", serverName, e.getMessage());
        return List.of();
    }
}

public record McpToolInfo(
    String name,
    String description,
    Map<String, Object> inputSchema,
    String serverName
) {}
```

**新增 API 端点：**

```java
// ToolController.java

@GetMapping("/mcp/tools")
public List<McpToolRegistrar.McpToolInfo> listMcpTools(
    @RequestParam(required = false) String server
) {
    if (server != null && !server.isBlank()) {
        return mcpToolRegistrar.enumerateTools(server);
    }
    
    // 枚举所有 MCP Server 的工具
    return mcpConfigs.stream()
        .map(config -> (String) config.get("server"))
        .filter(Objects::nonNull)
        .flatMap(serverName -> mcpToolRegistrar.enumerateTools(serverName).stream())
        .collect(Collectors.toList());
}
```

---

### 3.3 DB 初始化契约不清

**修复方案：** 新增启动检查

```java
package io.agentmanager.framework.config;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.boot.context.event.ApplicationReadyEvent;
import org.springframework.context.event.EventListener;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.stereotype.Component;

@Component
public class DatabaseHealthChecker {
    private static final Logger log = LoggerFactory.getLogger(DatabaseHealthChecker.class);
    
    private final JdbcTemplate jdbcTemplate;
    
    public DatabaseHealthChecker(JdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
    }
    
    @EventListener(ApplicationReadyEvent.class)
    public void checkDatabase() {
        log.info("Checking database connectivity...");
        
        try {
            // 检查连接
            jdbcTemplate.queryForObject("SELECT 1", Integer.class);
            log.info("Database connection: OK");
            
            // 检查表是否存在
            checkTables();
            
        } catch (Exception e) {
            log.error("Database check failed: {}", e.getMessage());
            log.error("Please ensure database exists and is accessible");
            log.error("Required: CREATE DATABASE IF NOT EXISTS agent_manager_test;");
            throw new IllegalStateException("Database initialization failed", e);
        }
    }
    
    private void checkTables() {
        var tables = List.of("agent_state", "agent_fs");
        for (var table : tables) {
            try {
                jdbcTemplate.queryForObject(
                    "SELECT COUNT(*) FROM " + table + " LIMIT 1",
                    Integer.class
                );
                log.info("Table '{}': OK", table);
            } catch (Exception e) {
                log.warn("Table '{}' may not exist or is empty: {}", table, e.getMessage());
            }
        }
    }
}
```

---

## 4. 完整修复清单

### 4.1 文件修改清单

| 文件 | 修改内容 | 优先级 |
|------|---------|--------|
| `A2AController.java` | 修复 A2A 协议参数解析 | P1 |
| `AgentRuntimeService.java` | 集成 SessionManager | P1 |
| `SessionManager.java` | **新增** 会话管理 | P1 |
| `SessionCleanupService.java` | **新增** 定时清理 | P1 |
| `ToolController.java` | 修复 /tools 端点 | P1 |
| `McpManager.java` | 修复 ActiveMCP.json 解析 | P1 |
| `McpToolRegistrar.java` | 添加工具查询方法 | P1 |
| `InfoController.java` | 新增 /metadata 端点 | P2 |
| `OafConfigLoader.java` | 修复 skills 解析 | P3 |
| `DatabaseHealthChecker.java` | **新增** 启动检查 | P3 |
| `Dockerfile` | 权限处理 | P2 |
| `AgentFrameworkApplication.java` | 启用 @EnableScheduling | P1 |

### 4.2 测试用例清单

```java
// A2AControllerTest.java

@Test
void testStandardA2AParams_TopLevel() {
    // 验证标准 A2A 顶层参数优先级
}

@Test
void testStandardA2AParams_BackwardCompatibility() {
    // 验证旧格式 metadata 参数兼容性
}

@Test
void testSessionResumption() {
    // 验证会话续接功能
}

// McpManagerTest.java

@Test
void testActiveMCPJson_NoException() {
    // 验证 ActiveMCP.json 不再导致 500
}

@Test
void testToolCountAccuracy() {
    // 验证 tool_count 准确性
}

// ToolControllerTest.java

@Test
void testListTools_ReturnsMcpTools() {
    // 验证 /tools 返回真实 MCP 工具
}

@Test
void testListTools_ExcludeInternal() {
    // 验证内置工具默认不暴露
}
```

---

## 5. 实施路线图

### Phase 1: 核心修复 (3-5 天)

| 任务 | 工作量 | 依赖 |
|------|--------|------|
| 修复 A2AController 参数解析 | 1d | 无 |
| 新增 SessionManager | 1d | 无 |
| 修复 McpManager ActiveMCP.json | 0.5d | 无 |
| 修复 ToolController /tools | 0.5d | 无 |
| 单元测试 | 1d | 以上所有 |

### Phase 2: 功能增强 (3-5 天)

| 任务 | 工作量 | 依赖 |
|------|--------|------|
| SessionCleanupService | 0.5d | SessionManager |
| /metadata 端点 | 0.5d | 无 |
| MCP 工具枚举 | 1d | McpToolRegistrar |
| 权限检查优化 | 0.5d | 无 |
| 集成测试 | 1d | 以上所有 |

### Phase 3: 体验优化 (2-3 天)

| 任务 | 工作量 | 依赖 |
|------|--------|------|
| Skills 模型增强 | 0.5d | 无 |
| DB 启动检查 | 0.5d | 无 |
| 文档更新 | 1d | 以上所有 |
| 回归测试 | 0.5d | 以上所有 |

---

## 6. 验证方案

### 6.1 手动验证

```bash
# 1. 验证 A2A 协议兼容性
curl -X POST http://localhost:8100/ \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "message/send",
    "params": {
      "userId": "test-user",
      "sessionId": "test-session",
      "message": {"role": "user", "parts": [{"text": "hello"}]}
    },
    "id": "req-1"
  }'

# 2. 验证 /tools 端点
curl http://localhost:8100/tools

# 3. 验证 /mcp 端点
curl http://localhost:8100/mcp

# 4. 验证 /metadata 端点
curl http://localhost:8100/metadata?includeDetails=true
```

### 6.2 自动化测试

```bash
cd agent-framework
mvn test
```

---

*文档版本: v1.0 | 最后更新: 2026-08-07*